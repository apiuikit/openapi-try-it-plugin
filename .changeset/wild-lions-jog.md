---
"@apiuikit/openapi-try-it-plugin": minor
---

Add `createTryItButtonPlugin`, a separate opt-in plugin that fills `openapi.operation.reference.supplementary` with a "Try it" button opening the request-sending panel in a modal. It does not register automatically alongside the existing `createTryItPlugin` tab — register either, both, or neither.
